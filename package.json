{
  "private": true,
  "name": "cc-developer-platform-pages",
  "version": "0.0.3",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/AdobeDocs/cc-developer-platform-pages"
  },
  "dependencies": {
    "@adobe/gatsby-theme-aio": "^3.0.0",
    "dev-site": "github:adobedocs/dev-site",
    "gatsby": "^3.0.0"
  },
  "scripts": {
    "start": "gatsby build && gatsby serve",
    "start:prefix": "gatsby build --prefix-paths && gatsby serve --prefix-paths",
    "dev": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  }
}
